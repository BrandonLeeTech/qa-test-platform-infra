# Conservative local-learning values for the official docker-selenium chart.
# Pin the chart version in the install command once a tested version is chosen.

isolateComponents: true

ingress:
  enabled: false

components:
  router:
    serviceType: ClusterIP

# Local-only convenience: allow noVNC Live View without a password.
# Do not use this setting when noVNC is exposed beyond the local machine.
chromeNode:
  extraEnvironmentVariables:
    - name: SE_VNC_NO_PASSWORD
      value: "true"

firefoxNode:
  extraEnvironmentVariables:
    - name: SE_VNC_NO_PASSWORD
      value: "true"

# Edge images are AMD64-only because Microsoft does not provide Edge for Linux/ARM.
# See: https://github.com/SeleniumHQ/docker-selenium#experimental-multi-arch-amd64aarch64armhf-images
edgeNode:
  enabled: false

autoscaling:
  enabled: false

basicAuth:
  enabled: false
